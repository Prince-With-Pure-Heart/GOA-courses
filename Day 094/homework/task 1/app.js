// *Filter Prime Numbers** - Create an array of numbers and use `filter()` to return only the prime numbers.



// **Map to Object Conversion**- Given an array of objects (e.g., users with `id` and `name`), use `map()` to create an array of strings that contains only the names.
const objects = [ id = {
    'h1': 'id'
    
}, p = {
    p1: 'p1'
}]